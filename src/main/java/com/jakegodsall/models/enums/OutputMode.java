package com.jakegodsall.models.enums;

/**
 * This class represents the output mode for the flashcard generator.
 */
public enum OutputMode {
    /**
     * Used to generate CSV files.
     */
    CSV,

    /**
     * Used to generate JSON files.
     */
    JSON
}
